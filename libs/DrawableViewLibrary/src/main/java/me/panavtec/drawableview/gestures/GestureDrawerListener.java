package me.panavtec.drawableview.gestures;

import me.panavtec.drawableview.internal.SerializablePath;

public interface GestureDrawerListener {
  void onGestureDrawedOk(SerializablePath serializablePath);
}
